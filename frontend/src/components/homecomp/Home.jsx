import React, { Component } from 'react'

export default class Home extends Component {
    render(props) {
        // const introDaycare = props.daycares.map( daycare => <li key={daycare.id}>{daycare}</li>)
        return (
            <div>
                {/* {introDaycare} */}
            </div>
        )
    }
}
